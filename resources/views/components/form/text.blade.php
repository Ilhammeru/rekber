<input type="{{ $type }}"
    class="form-control {{ $className }}"
    name="{{ $name }}"
    id="{{ $id }}"
    {{ $attributes }}
    placeholder="{{ $text }}">
<div class="invalid-feedback" id="error-{{ $error }}"></div>
